Read a previously-stored variable.

Args: `{ name }`

  name: the variable name to look up.

Returns the stored JSON value rendered as text (objects/arrays pretty-printed with two-space indent). Errors if no variable with that name has been set.
