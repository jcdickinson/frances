TODO: how do we restore scrollback: we want to restore it as committed (so it can be explored, but does not go to the tty).

blocks are stored in the DB when marked as safe-to-commit, and assosciated with a workflow?
