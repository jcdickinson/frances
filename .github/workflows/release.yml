name: build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
    inputs:
      release:
        description: "Tag to publish a draft release for (leave empty to skip)"
        required: false
        type: string

permissions:
  contents: write

jobs:
  build:
    name: ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-musl
            runner: ubuntu-24.04
          - target: aarch64-unknown-linux-musl
            runner: ubuntu-24.04-arm
          - target: x86_64-apple-darwin
            runner: macos-15-intel
          - target: aarch64-apple-darwin
            runner: macos-14

    steps:
      - uses: actions/checkout@v4

      # musl needs a C toolchain for the bundled QuickJS (rquickjs). On the
      # native arm64 runner, musl-gcc from musl-tools targets aarch64.
      - name: Install musl toolchain
        if: contains(matrix.target, '-linux-musl')
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools
          key=$(echo '${{ matrix.target }}' | tr '-' '_')
          echo "CC_${key}=musl-gcc" >> "$GITHUB_ENV"
          echo "CARGO_TARGET_${key^^}_LINKER=musl-gcc" >> "$GITHUB_ENV"

      - name: Install Rust target
        run: rustup target add ${{ matrix.target }}

      - name: Build
        run: cargo build --release --locked -p frances --target ${{ matrix.target }}

      - name: Package
        id: package
        run: |
          name="frances-${{ matrix.target }}"
          staging="$name"
          mkdir "$staging"
          cp "target/${{ matrix.target }}/release/frances" "$staging/"
          tar czf "$name.tar.gz" "$staging"
          echo "asset=$name.tar.gz" >> "$GITHUB_OUTPUT"

      - uses: actions/upload-artifact@v4
        with:
          name: frances-${{ matrix.target }}
          path: ${{ steps.package.outputs.asset }}

      - name: Attach to release
        if: inputs.release != ''
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.package.outputs.asset }}
          tag_name: ${{ inputs.release }}
          draft: true
